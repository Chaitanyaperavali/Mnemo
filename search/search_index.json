{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Mnemo","text":"<p>LLM agent memory orchestration library.</p> <p>Mnemo provides a backend-agnostic, async-first API for storing, retrieving, and managing memories for LLM agents.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Async-first design with a thin sync shim for non-async callers</li> <li>Backend-agnostic via <code>Protocol</code> \u2014 bring your own vector store</li> <li>Typed from top to bottom (<code>py.typed</code> marker, strict mypy)</li> <li>Ships with an in-memory backend for tests and prototyping</li> </ul>"},{"location":"#quick-example","title":"Quick example","text":"<pre><code>import asyncio\nfrom mnemo import MemoryStore\nfrom mnemo.backends import InMemoryBackend\nfrom mnemo.types import Memory, MemoryQuery\n\nasync def main() -&gt; None:\n    async with MemoryStore(backend=InMemoryBackend()) as store:\n        mid = await store.add(Memory(content=\"Paris is the capital of France\"))\n        results = await store.search(MemoryQuery(text=\"capital of France\"))\n        print(results[0].memory.content)\n\nasyncio.run(main())\n</code></pre>"},{"location":"api/","title":"API Reference","text":""},{"location":"api/#mnemo","title":"mnemo","text":"<p>Mnemo \u2014 LLM agent memory orchestration library.</p> <p>Provides a backend-agnostic, async-first API for storing, retrieving, and managing memories for LLM agents.</p> Example <p>from mnemo import MemoryStore store = MemoryStore(backend=InMemoryBackend())</p>"},{"location":"api/#mnemo.MemoryStore","title":"MemoryStore","text":"<pre><code>MemoryStore(backend: MemoryBackend)\n</code></pre> <p>Async-first, backend-agnostic memory store.</p> <p>Parameters:</p> Name Type Description Default <code>backend</code> <code>MemoryBackend</code> <p>Any object satisfying the :class:<code>~mnemo.protocols.MemoryBackend</code> protocol.</p> required Example <p>async def main(): ...     async with MemoryStore(backend=InMemoryBackend()) as store: ...         mid = await store.add(Memory(content=\"hello world\")) ...         result = await store.get(mid)</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def __init__(self, backend: MemoryBackend) -&gt; None:\n    self._backend = backend\n</code></pre>"},{"location":"api/#mnemo.MemoryStore.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Close the underlying backend and release all resources.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Close the underlying backend and release all resources.\"\"\"\n    await self._backend.aclose()\n</code></pre>"},{"location":"api/#mnemo.MemoryStore.add","title":"add  <code>async</code>","text":"<pre><code>add(memory: Memory) -&gt; str\n</code></pre> <p>Add a memory to the store.</p> <p>Parameters:</p> Name Type Description Default <code>memory</code> <code>Memory</code> <p>The memory to persist.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The unique ID assigned to the stored memory.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def add(self, memory: Memory) -&gt; str:\n    \"\"\"Add a memory to the store.\n\n    Args:\n        memory: The memory to persist.\n\n    Returns:\n        The unique ID assigned to the stored memory.\n    \"\"\"\n    return await self._backend.add(memory)\n</code></pre>"},{"location":"api/#mnemo.MemoryStore.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(memory_id: str) -&gt; bool\n</code></pre> <p>Delete a memory by ID.</p> <p>Parameters:</p> Name Type Description Default <code>memory_id</code> <code>str</code> <p>The ID to remove.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if deleted, <code>False</code> if not found.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def delete(self, memory_id: str) -&gt; bool:\n    \"\"\"Delete a memory by ID.\n\n    Args:\n        memory_id: The ID to remove.\n\n    Returns:\n        ``True`` if deleted, ``False`` if not found.\n    \"\"\"\n    return await self._backend.delete(memory_id)\n</code></pre>"},{"location":"api/#mnemo.MemoryStore.get","title":"get  <code>async</code>","text":"<pre><code>get(memory_id: str) -&gt; Memory | None\n</code></pre> <p>Retrieve a memory by ID.</p> <p>Parameters:</p> Name Type Description Default <code>memory_id</code> <code>str</code> <p>The ID returned by :meth:<code>add</code>.</p> required <p>Returns:</p> Type Description <code>Memory | None</code> <p>The memory, or <code>None</code> if not found.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def get(self, memory_id: str) -&gt; Memory | None:\n    \"\"\"Retrieve a memory by ID.\n\n    Args:\n        memory_id: The ID returned by :meth:`add`.\n\n    Returns:\n        The memory, or ``None`` if not found.\n    \"\"\"\n    return await self._backend.get(memory_id)\n</code></pre>"},{"location":"api/#mnemo.MemoryStore.search","title":"search  <code>async</code>","text":"<pre><code>search(query: MemoryQuery) -&gt; list[MemoryResult]\n</code></pre> <p>Search memories by semantic similarity and/or metadata filters.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>MemoryQuery</code> <p>A :class:<code>~mnemo.types.MemoryQuery</code> describing what to find.</p> required <p>Returns:</p> Type Description <code>list[MemoryResult]</code> <p>A ranked list of :class:<code>~mnemo.types.MemoryResult</code> objects.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n    \"\"\"Search memories by semantic similarity and/or metadata filters.\n\n    Args:\n        query: A :class:`~mnemo.types.MemoryQuery` describing what to find.\n\n    Returns:\n        A ranked list of :class:`~mnemo.types.MemoryResult` objects.\n    \"\"\"\n    return await self._backend.search(query)\n</code></pre>"},{"location":"api/#mnemo.backends","title":"backends","text":"<p>Built-in backend implementations.</p>"},{"location":"api/#mnemo.backends.InMemoryBackend","title":"InMemoryBackend","text":"<pre><code>InMemoryBackend()\n</code></pre> <p>               Bases: <code>BaseBackend</code></p> <p>A non-persistent in-process backend backed by a plain dict.</p> <p>Thread-safe reads are fine; this backend is not designed for concurrent writes across OS threads.</p> Example <p>import anyio async def demo(): ...     async with InMemoryBackend() as backend: ...         mid = await backend.add(Memory(content=\"hello\")) ...         mem = await backend.get(mid) ...         assert mem is not None</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>def __init__(self) -&gt; None:\n    super().__init__()\n    self._store: dict[str, Memory] = {}\n</code></pre>"},{"location":"api/#mnemo.backends.InMemoryBackend.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Clear the store and mark the backend as closed.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Clear the store and mark the backend as closed.\"\"\"\n    self._store.clear()\n    self._closed = True\n</code></pre>"},{"location":"api/#mnemo.backends.InMemoryBackend.add","title":"add  <code>async</code>","text":"<pre><code>add(memory: Memory) -&gt; str\n</code></pre> <p>Persist a memory and return its assigned ID.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def add(self, memory: Memory) -&gt; str:\n    \"\"\"Persist a memory and return its assigned ID.\"\"\"\n    self._check_not_closed()\n    mid = memory.memory_id or str(uuid.uuid4())\n    # Store a fresh copy with the assigned ID\n    self._store[mid] = Memory(\n        content=memory.content,\n        metadata=memory.metadata,\n        embedding=memory.embedding,\n        memory_id=mid,\n    )\n    return mid\n</code></pre>"},{"location":"api/#mnemo.backends.InMemoryBackend.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(memory_id: str) -&gt; bool\n</code></pre> <p>Remove a memory by ID, returning True if it existed.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def delete(self, memory_id: str) -&gt; bool:\n    \"\"\"Remove a memory by ID, returning True if it existed.\"\"\"\n    self._check_not_closed()\n    return self._store.pop(memory_id, None) is not None\n</code></pre>"},{"location":"api/#mnemo.backends.InMemoryBackend.get","title":"get  <code>async</code>","text":"<pre><code>get(memory_id: str) -&gt; Memory | None\n</code></pre> <p>Retrieve a single memory by its ID.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def get(self, memory_id: str) -&gt; Memory | None:\n    \"\"\"Retrieve a single memory by its ID.\"\"\"\n    self._check_not_closed()\n    return self._store.get(memory_id)\n</code></pre>"},{"location":"api/#mnemo.backends.InMemoryBackend.search","title":"search  <code>async</code>","text":"<pre><code>search(query: MemoryQuery) -&gt; list[MemoryResult]\n</code></pre> <p>Na\u00efve substring search \u2014 replace with vector similarity in real backends.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n    \"\"\"Na\u00efve substring search \u2014 replace with vector similarity in real backends.\"\"\"\n    self._check_not_closed()\n    results: list[MemoryResult] = []\n    for memory in self._store.values():\n        # Apply metadata filters\n        if not all(memory.metadata.get(k) == v for k, v in query.filters.items()):\n            continue\n        # Na\u00efve scoring: 1.0 if substring match, 0.5 otherwise\n        score = 1.0 if query.text.lower() in memory.content.lower() else 0.5\n        if score &gt;= query.score_threshold:\n            results.append(MemoryResult(memory=memory, score=score))\n\n    results.sort(key=lambda r: r.score, reverse=True)\n    return results[: query.limit]\n</code></pre>"},{"location":"api/#mnemo.backends.memory","title":"memory","text":"<p>In-memory backend \u2014 useful for tests and prototyping.</p>"},{"location":"api/#mnemo.backends.memory.InMemoryBackend","title":"InMemoryBackend","text":"<pre><code>InMemoryBackend()\n</code></pre> <p>               Bases: <code>BaseBackend</code></p> <p>A non-persistent in-process backend backed by a plain dict.</p> <p>Thread-safe reads are fine; this backend is not designed for concurrent writes across OS threads.</p> Example <p>import anyio async def demo(): ...     async with InMemoryBackend() as backend: ...         mid = await backend.add(Memory(content=\"hello\")) ...         mem = await backend.get(mid) ...         assert mem is not None</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>def __init__(self) -&gt; None:\n    super().__init__()\n    self._store: dict[str, Memory] = {}\n</code></pre>"},{"location":"api/#mnemo.backends.memory.InMemoryBackend.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Clear the store and mark the backend as closed.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Clear the store and mark the backend as closed.\"\"\"\n    self._store.clear()\n    self._closed = True\n</code></pre>"},{"location":"api/#mnemo.backends.memory.InMemoryBackend.add","title":"add  <code>async</code>","text":"<pre><code>add(memory: Memory) -&gt; str\n</code></pre> <p>Persist a memory and return its assigned ID.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def add(self, memory: Memory) -&gt; str:\n    \"\"\"Persist a memory and return its assigned ID.\"\"\"\n    self._check_not_closed()\n    mid = memory.memory_id or str(uuid.uuid4())\n    # Store a fresh copy with the assigned ID\n    self._store[mid] = Memory(\n        content=memory.content,\n        metadata=memory.metadata,\n        embedding=memory.embedding,\n        memory_id=mid,\n    )\n    return mid\n</code></pre>"},{"location":"api/#mnemo.backends.memory.InMemoryBackend.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(memory_id: str) -&gt; bool\n</code></pre> <p>Remove a memory by ID, returning True if it existed.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def delete(self, memory_id: str) -&gt; bool:\n    \"\"\"Remove a memory by ID, returning True if it existed.\"\"\"\n    self._check_not_closed()\n    return self._store.pop(memory_id, None) is not None\n</code></pre>"},{"location":"api/#mnemo.backends.memory.InMemoryBackend.get","title":"get  <code>async</code>","text":"<pre><code>get(memory_id: str) -&gt; Memory | None\n</code></pre> <p>Retrieve a single memory by its ID.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def get(self, memory_id: str) -&gt; Memory | None:\n    \"\"\"Retrieve a single memory by its ID.\"\"\"\n    self._check_not_closed()\n    return self._store.get(memory_id)\n</code></pre>"},{"location":"api/#mnemo.backends.memory.InMemoryBackend.search","title":"search  <code>async</code>","text":"<pre><code>search(query: MemoryQuery) -&gt; list[MemoryResult]\n</code></pre> <p>Na\u00efve substring search \u2014 replace with vector similarity in real backends.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n    \"\"\"Na\u00efve substring search \u2014 replace with vector similarity in real backends.\"\"\"\n    self._check_not_closed()\n    results: list[MemoryResult] = []\n    for memory in self._store.values():\n        # Apply metadata filters\n        if not all(memory.metadata.get(k) == v for k, v in query.filters.items()):\n            continue\n        # Na\u00efve scoring: 1.0 if substring match, 0.5 otherwise\n        score = 1.0 if query.text.lower() in memory.content.lower() else 0.5\n        if score &gt;= query.score_threshold:\n            results.append(MemoryResult(memory=memory, score=score))\n\n    results.sort(key=lambda r: r.score, reverse=True)\n    return results[: query.limit]\n</code></pre>"},{"location":"api/#mnemo.protocols","title":"protocols","text":"<p>Core Protocol definitions for the mnemo plugin interface.</p>"},{"location":"api/#mnemo.protocols--design-rationale","title":"Design rationale","text":"<p>We use <code>Protocol</code> (PEP 544) for the external plugin boundary so that third-party backends never need to import from <code>mnemo</code> at all \u2014 they just need to satisfy the structural shape.  This avoids a circular import trap and keeps the barrier to writing a custom backend as low as possible.</p> <p>We use <code>ABC</code> only for the internal <code>BaseBackend</code> helper class that provides shared validation + lifecycle logic that concrete backends inherit. Third-party authors CAN subclass it for convenience, but they are not required to.</p> <p>Runtime checking via <code>@runtime_checkable</code> is intentionally kept on the Protocol so you can do <code>isinstance(obj, MemoryBackend)</code> in tests.</p>"},{"location":"api/#mnemo.protocols.BaseBackend","title":"BaseBackend","text":"<pre><code>BaseBackend()\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Internal ABC for backends shipped with mnemo.</p> <p>Third-party backends are NOT required to inherit from this class; they only need to satisfy :class:<code>MemoryBackend</code>.  This class exists to centralise validation, lifecycle hooks, and telemetry for the first-party backends.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>def __init__(self) -&gt; None:\n    self._closed = False\n</code></pre>"},{"location":"api/#mnemo.protocols.BaseBackend.aclose","title":"aclose  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Release resources.  Subclasses must call <code>self._closed = True</code>.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>@abc.abstractmethod\nasync def aclose(self) -&gt; None:\n    \"\"\"Release resources.  Subclasses must call ``self._closed = True``.\"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.protocols.BaseBackend.add","title":"add  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>add(memory: Memory) -&gt; str\n</code></pre> <p>Persist a memory and return its assigned ID.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>@abc.abstractmethod\nasync def add(self, memory: Memory) -&gt; str:\n    \"\"\"Persist a memory and return its assigned ID.\"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.protocols.BaseBackend.delete","title":"delete  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>delete(memory_id: str) -&gt; bool\n</code></pre> <p>Remove a memory by ID, returning True if it existed.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>@abc.abstractmethod\nasync def delete(self, memory_id: str) -&gt; bool:\n    \"\"\"Remove a memory by ID, returning True if it existed.\"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.protocols.BaseBackend.get","title":"get  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get(memory_id: str) -&gt; Memory | None\n</code></pre> <p>Retrieve a single memory by its ID.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>@abc.abstractmethod\nasync def get(self, memory_id: str) -&gt; Memory | None:\n    \"\"\"Retrieve a single memory by its ID.\"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.protocols.BaseBackend.search","title":"search  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>search(query: MemoryQuery) -&gt; list[MemoryResult]\n</code></pre> <p>Return memories ranked by relevance to the query.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>@abc.abstractmethod\nasync def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n    \"\"\"Return memories ranked by relevance to the query.\"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.protocols.MemoryBackend","title":"MemoryBackend","text":"<p>               Bases: <code>Protocol</code></p> <p>Structural interface for all mnemo storage backends.</p> <p>Implementors do NOT need to inherit from this class.  A class satisfies this protocol if it defines all methods with the matching signatures.</p> <p>Parameters:</p> Name Type Description Default <code>Protocol</code> <p>Python's built-in structural typing base.</p> required Example <p>class MyBackend: ...     async def add(self, memory: Memory) -&gt; str: ... ...     async def get(self, memory_id: str) -&gt; Memory | None: ... ...     async def search( ...         self, query: MemoryQuery ...     ) -&gt; list[MemoryResult]: ... ...     async def delete(self, memory_id: str) -&gt; bool: ... ...     async def aclose(self) -&gt; None: ... isinstance(MyBackend(), MemoryBackend) True</p>"},{"location":"api/#mnemo.protocols.MemoryBackend.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Release all resources held by the backend.</p> <p>Called automatically when used as an async context manager.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Release all resources held by the backend.\n\n    Called automatically when used as an async context manager.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.protocols.MemoryBackend.add","title":"add  <code>async</code>","text":"<pre><code>add(memory: Memory) -&gt; str\n</code></pre> <p>Persist a memory and return its assigned ID.</p> <p>Parameters:</p> Name Type Description Default <code>memory</code> <code>Memory</code> <p>The memory object to store.</p> required <p>Returns:</p> Type Description <code>str</code> <p>A unique identifier string for the stored memory.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def add(self, memory: Memory) -&gt; str:\n    \"\"\"Persist a memory and return its assigned ID.\n\n    Args:\n        memory: The memory object to store.\n\n    Returns:\n        A unique identifier string for the stored memory.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.protocols.MemoryBackend.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(memory_id: str) -&gt; bool\n</code></pre> <p>Remove a memory by ID.</p> <p>Parameters:</p> Name Type Description Default <code>memory_id</code> <code>str</code> <p>The ID to delete.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the memory existed and was removed, <code>False</code></p> <code>bool</code> <p>if it was not found.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def delete(self, memory_id: str) -&gt; bool:\n    \"\"\"Remove a memory by ID.\n\n    Args:\n        memory_id: The ID to delete.\n\n    Returns:\n        ``True`` if the memory existed and was removed, ``False``\n        if it was not found.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.protocols.MemoryBackend.get","title":"get  <code>async</code>","text":"<pre><code>get(memory_id: str) -&gt; Memory | None\n</code></pre> <p>Retrieve a single memory by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>memory_id</code> <code>str</code> <p>The unique identifier returned by :meth:<code>add</code>.</p> required <p>Returns:</p> Type Description <code>Memory | None</code> <p>The memory if found, otherwise <code>None</code>.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def get(self, memory_id: str) -&gt; Memory | None:\n    \"\"\"Retrieve a single memory by its ID.\n\n    Args:\n        memory_id: The unique identifier returned by :meth:`add`.\n\n    Returns:\n        The memory if found, otherwise ``None``.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.protocols.MemoryBackend.search","title":"search  <code>async</code>","text":"<pre><code>search(query: MemoryQuery) -&gt; list[MemoryResult]\n</code></pre> <p>Return memories ranked by relevance to the query.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>MemoryQuery</code> <p>Structured query containing text, filters, and limit.</p> required <p>Returns:</p> Type Description <code>list[MemoryResult]</code> <p>A list of results ordered by descending relevance score.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n    \"\"\"Return memories ranked by relevance to the query.\n\n    Args:\n        query: Structured query containing text, filters, and limit.\n\n    Returns:\n        A list of results ordered by descending relevance score.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.protocols.SupportsBulkOperations","title":"SupportsBulkOperations","text":"<p>               Bases: <code>Protocol</code></p> <p>Optional protocol for backends with efficient batch APIs.</p>"},{"location":"api/#mnemo.protocols.SupportsBulkOperations.add_many","title":"add_many  <code>async</code>","text":"<pre><code>add_many(memories: Sequence[Memory]) -&gt; list[str]\n</code></pre> <p>Persist multiple memories atomically.</p> <p>Parameters:</p> Name Type Description Default <code>memories</code> <code>Sequence[Memory]</code> <p>An ordered sequence of memories to store.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of IDs in the same order as the input.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def add_many(self, memories: Sequence[Memory]) -&gt; list[str]:\n    \"\"\"Persist multiple memories atomically.\n\n    Args:\n        memories: An ordered sequence of memories to store.\n\n    Returns:\n        A list of IDs in the same order as the input.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.protocols.SupportsBulkOperations.delete_many","title":"delete_many  <code>async</code>","text":"<pre><code>delete_many(memory_ids: Sequence[str]) -&gt; int\n</code></pre> <p>Delete multiple memories in one operation.</p> <p>Parameters:</p> Name Type Description Default <code>memory_ids</code> <code>Sequence[str]</code> <p>IDs to remove.</p> required <p>Returns:</p> Type Description <code>int</code> <p>The count of successfully deleted memories.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def delete_many(self, memory_ids: Sequence[str]) -&gt; int:\n    \"\"\"Delete multiple memories in one operation.\n\n    Args:\n        memory_ids: IDs to remove.\n\n    Returns:\n        The count of successfully deleted memories.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.protocols.SupportsStreaming","title":"SupportsStreaming","text":"<p>               Bases: <code>Protocol</code></p> <p>Optional protocol for backends that can stream search results.</p>"},{"location":"api/#mnemo.protocols.SupportsStreaming.stream_search","title":"stream_search","text":"<pre><code>stream_search(\n    query: MemoryQuery,\n) -&gt; AsyncIterator[MemoryResult]\n</code></pre> <p>Yield results one at a time as they become available.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>MemoryQuery</code> <p>Structured query.</p> required <p>Yields:</p> Type Description <code>AsyncIterator[MemoryResult]</code> <p>Individual memory results in relevance order.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>def stream_search(self, query: MemoryQuery) -&gt; AsyncIterator[MemoryResult]:\n    \"\"\"Yield results one at a time as they become available.\n\n    Args:\n        query: Structured query.\n\n    Yields:\n        Individual memory results in relevance order.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/#mnemo.store","title":"store","text":"<p>High-level MemoryStore: the primary user-facing API.</p>"},{"location":"api/#mnemo.store--asyncsync-dual-api-pattern","title":"Async/sync dual-API pattern","text":"<p><code>MemoryStore</code> is async-first.  Every public method is a coroutine. For callers in a synchronous context we expose a thin <code>SyncMemoryStore</code> wrapper that uses <code>anyio.from_thread.run_sync</code> / <code>anyio.run</code> to bridge into the async layer without the user needing to manage an event loop.</p> <p>The pattern here follows the recommendation in Seth Larson's \"Designing Libraries for Async and Sync I/O\":</p> <ol> <li>The real implementation lives in the async class.</li> <li>The sync wrapper is a thin shim \u2014 it does NOT duplicate logic.</li> <li>We accept AnyIO so both asyncio and Trio users work out of the box.</li> </ol>"},{"location":"api/#mnemo.store.MemoryStore","title":"MemoryStore","text":"<pre><code>MemoryStore(backend: MemoryBackend)\n</code></pre> <p>Async-first, backend-agnostic memory store.</p> <p>Parameters:</p> Name Type Description Default <code>backend</code> <code>MemoryBackend</code> <p>Any object satisfying the :class:<code>~mnemo.protocols.MemoryBackend</code> protocol.</p> required Example <p>async def main(): ...     async with MemoryStore(backend=InMemoryBackend()) as store: ...         mid = await store.add(Memory(content=\"hello world\")) ...         result = await store.get(mid)</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def __init__(self, backend: MemoryBackend) -&gt; None:\n    self._backend = backend\n</code></pre>"},{"location":"api/#mnemo.store.MemoryStore.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Close the underlying backend and release all resources.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Close the underlying backend and release all resources.\"\"\"\n    await self._backend.aclose()\n</code></pre>"},{"location":"api/#mnemo.store.MemoryStore.add","title":"add  <code>async</code>","text":"<pre><code>add(memory: Memory) -&gt; str\n</code></pre> <p>Add a memory to the store.</p> <p>Parameters:</p> Name Type Description Default <code>memory</code> <code>Memory</code> <p>The memory to persist.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The unique ID assigned to the stored memory.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def add(self, memory: Memory) -&gt; str:\n    \"\"\"Add a memory to the store.\n\n    Args:\n        memory: The memory to persist.\n\n    Returns:\n        The unique ID assigned to the stored memory.\n    \"\"\"\n    return await self._backend.add(memory)\n</code></pre>"},{"location":"api/#mnemo.store.MemoryStore.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(memory_id: str) -&gt; bool\n</code></pre> <p>Delete a memory by ID.</p> <p>Parameters:</p> Name Type Description Default <code>memory_id</code> <code>str</code> <p>The ID to remove.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if deleted, <code>False</code> if not found.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def delete(self, memory_id: str) -&gt; bool:\n    \"\"\"Delete a memory by ID.\n\n    Args:\n        memory_id: The ID to remove.\n\n    Returns:\n        ``True`` if deleted, ``False`` if not found.\n    \"\"\"\n    return await self._backend.delete(memory_id)\n</code></pre>"},{"location":"api/#mnemo.store.MemoryStore.get","title":"get  <code>async</code>","text":"<pre><code>get(memory_id: str) -&gt; Memory | None\n</code></pre> <p>Retrieve a memory by ID.</p> <p>Parameters:</p> Name Type Description Default <code>memory_id</code> <code>str</code> <p>The ID returned by :meth:<code>add</code>.</p> required <p>Returns:</p> Type Description <code>Memory | None</code> <p>The memory, or <code>None</code> if not found.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def get(self, memory_id: str) -&gt; Memory | None:\n    \"\"\"Retrieve a memory by ID.\n\n    Args:\n        memory_id: The ID returned by :meth:`add`.\n\n    Returns:\n        The memory, or ``None`` if not found.\n    \"\"\"\n    return await self._backend.get(memory_id)\n</code></pre>"},{"location":"api/#mnemo.store.MemoryStore.search","title":"search  <code>async</code>","text":"<pre><code>search(query: MemoryQuery) -&gt; list[MemoryResult]\n</code></pre> <p>Search memories by semantic similarity and/or metadata filters.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>MemoryQuery</code> <p>A :class:<code>~mnemo.types.MemoryQuery</code> describing what to find.</p> required <p>Returns:</p> Type Description <code>list[MemoryResult]</code> <p>A ranked list of :class:<code>~mnemo.types.MemoryResult</code> objects.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n    \"\"\"Search memories by semantic similarity and/or metadata filters.\n\n    Args:\n        query: A :class:`~mnemo.types.MemoryQuery` describing what to find.\n\n    Returns:\n        A ranked list of :class:`~mnemo.types.MemoryResult` objects.\n    \"\"\"\n    return await self._backend.search(query)\n</code></pre>"},{"location":"api/#mnemo.store.SyncMemoryStore","title":"SyncMemoryStore","text":"<pre><code>SyncMemoryStore(backend: MemoryBackend)\n</code></pre> <p>Synchronous fa\u00e7ade over :class:<code>MemoryStore</code>.</p> <p>Intended for callers that cannot use <code>async</code>/<code>await</code> (e.g., Jupyter cells in <code>%autoawait off</code> mode, Django views, CLI scripts).</p> <p>Internally this wraps every call with <code>anyio.run()</code>, so it is compatible with both asyncio and Trio backends.</p> <p>Parameters:</p> Name Type Description Default <code>backend</code> <code>MemoryBackend</code> <p>Any object satisfying :class:<code>~mnemo.protocols.MemoryBackend</code>.</p> required Example <p>store = SyncMemoryStore(backend=InMemoryBackend()) with store: ...     mid = store.add(Memory(content=\"hello\")) ...     mem = store.get(mid)</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def __init__(self, backend: MemoryBackend) -&gt; None:\n    self._async_store = MemoryStore(backend)\n</code></pre>"},{"location":"api/#mnemo.store.SyncMemoryStore.add","title":"add","text":"<pre><code>add(memory: Memory) -&gt; str\n</code></pre> <p>Synchronous version of :meth:<code>MemoryStore.add</code>.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def add(self, memory: Memory) -&gt; str:\n    \"\"\"Synchronous version of :meth:`MemoryStore.add`.\"\"\"\n    return anyio.run(self._async_store.add, memory)\n</code></pre>"},{"location":"api/#mnemo.store.SyncMemoryStore.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close the underlying backend.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close the underlying backend.\"\"\"\n    anyio.run(self._async_store.aclose)\n</code></pre>"},{"location":"api/#mnemo.store.SyncMemoryStore.delete","title":"delete","text":"<pre><code>delete(memory_id: str) -&gt; bool\n</code></pre> <p>Synchronous version of :meth:<code>MemoryStore.delete</code>.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def delete(self, memory_id: str) -&gt; bool:\n    \"\"\"Synchronous version of :meth:`MemoryStore.delete`.\"\"\"\n    return anyio.run(self._async_store.delete, memory_id)\n</code></pre>"},{"location":"api/#mnemo.store.SyncMemoryStore.get","title":"get","text":"<pre><code>get(memory_id: str) -&gt; Memory | None\n</code></pre> <p>Synchronous version of :meth:<code>MemoryStore.get</code>.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def get(self, memory_id: str) -&gt; Memory | None:\n    \"\"\"Synchronous version of :meth:`MemoryStore.get`.\"\"\"\n    return anyio.run(self._async_store.get, memory_id)\n</code></pre>"},{"location":"api/#mnemo.store.SyncMemoryStore.search","title":"search","text":"<pre><code>search(query: MemoryQuery) -&gt; list[MemoryResult]\n</code></pre> <p>Synchronous version of :meth:<code>MemoryStore.search</code>.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n    \"\"\"Synchronous version of :meth:`MemoryStore.search`.\"\"\"\n    return anyio.run(self._async_store.search, query)\n</code></pre>"},{"location":"api/#mnemo.types","title":"types","text":"<p>Shared domain types for mnemo.</p> <p>All types are plain dataclasses or TypedDicts so that backends can import them without pulling in heavy dependencies.</p>"},{"location":"api/#mnemo.types.Memory","title":"Memory  <code>dataclass</code>","text":"<pre><code>Memory(\n    content: str,\n    metadata: dict[str, object] = _str_object_dict(),\n    embedding: list[float] | None = None,\n    memory_id: str | None = None,\n)\n</code></pre> <p>An immutable memory record.</p> <p>Parameters:</p> Name Type Description Default <code>content</code> <code>str</code> <p>The raw text (or serialised) content of the memory.</p> required <code>metadata</code> <code>dict[str, object]</code> <p>Arbitrary key-value pairs attached to this memory.</p> <code>_str_object_dict()</code> <code>embedding</code> <code>list[float] | None</code> <p>Optional pre-computed embedding vector.</p> <code>None</code> <code>memory_id</code> <code>str | None</code> <p>Optional caller-supplied ID; backends may override it.</p> <code>None</code>"},{"location":"api/#mnemo.types.MemoryQuery","title":"MemoryQuery  <code>dataclass</code>","text":"<pre><code>MemoryQuery(\n    text: str,\n    filters: dict[str, object] = _str_object_dict(),\n    limit: int = 10,\n    score_threshold: float = 0.0,\n)\n</code></pre> <p>A structured search query.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Natural-language text to search for.</p> required <code>filters</code> <code>dict[str, object]</code> <p>Key-value metadata filters (AND logic).</p> <code>_str_object_dict()</code> <code>limit</code> <code>int</code> <p>Maximum number of results to return.</p> <code>10</code> <code>score_threshold</code> <code>float</code> <p>Minimum similarity score (0.0-1.0).</p> <code>0.0</code>"},{"location":"api/#mnemo.types.MemoryResult","title":"MemoryResult  <code>dataclass</code>","text":"<pre><code>MemoryResult(memory: Memory, score: float)\n</code></pre> <p>A search result pairing a memory with its relevance score.</p> <p>Parameters:</p> Name Type Description Default <code>memory</code> <code>Memory</code> <p>The retrieved memory.</p> required <code>score</code> <code>float</code> <p>Similarity score in the range [0.0, 1.0].</p> required"},{"location":"api/protocols/","title":"Protocols","text":""},{"location":"api/protocols/#mnemo.protocols.MemoryBackend","title":"MemoryBackend","text":"<p>               Bases: <code>Protocol</code></p> <p>Structural interface for all mnemo storage backends.</p> <p>Implementors do NOT need to inherit from this class.  A class satisfies this protocol if it defines all methods with the matching signatures.</p> <p>Parameters:</p> Name Type Description Default <code>Protocol</code> <p>Python's built-in structural typing base.</p> required Example <p>class MyBackend: ...     async def add(self, memory: Memory) -&gt; str: ... ...     async def get(self, memory_id: str) -&gt; Memory | None: ... ...     async def search( ...         self, query: MemoryQuery ...     ) -&gt; list[MemoryResult]: ... ...     async def delete(self, memory_id: str) -&gt; bool: ... ...     async def aclose(self) -&gt; None: ... isinstance(MyBackend(), MemoryBackend) True</p>"},{"location":"api/protocols/#mnemo.protocols.MemoryBackend.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Release all resources held by the backend.</p> <p>Called automatically when used as an async context manager.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Release all resources held by the backend.\n\n    Called automatically when used as an async context manager.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.MemoryBackend.add","title":"add  <code>async</code>","text":"<pre><code>add(memory: Memory) -&gt; str\n</code></pre> <p>Persist a memory and return its assigned ID.</p> <p>Parameters:</p> Name Type Description Default <code>memory</code> <code>Memory</code> <p>The memory object to store.</p> required <p>Returns:</p> Type Description <code>str</code> <p>A unique identifier string for the stored memory.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def add(self, memory: Memory) -&gt; str:\n    \"\"\"Persist a memory and return its assigned ID.\n\n    Args:\n        memory: The memory object to store.\n\n    Returns:\n        A unique identifier string for the stored memory.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.MemoryBackend.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(memory_id: str) -&gt; bool\n</code></pre> <p>Remove a memory by ID.</p> <p>Parameters:</p> Name Type Description Default <code>memory_id</code> <code>str</code> <p>The ID to delete.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the memory existed and was removed, <code>False</code></p> <code>bool</code> <p>if it was not found.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def delete(self, memory_id: str) -&gt; bool:\n    \"\"\"Remove a memory by ID.\n\n    Args:\n        memory_id: The ID to delete.\n\n    Returns:\n        ``True`` if the memory existed and was removed, ``False``\n        if it was not found.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.MemoryBackend.get","title":"get  <code>async</code>","text":"<pre><code>get(memory_id: str) -&gt; Memory | None\n</code></pre> <p>Retrieve a single memory by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>memory_id</code> <code>str</code> <p>The unique identifier returned by :meth:<code>add</code>.</p> required <p>Returns:</p> Type Description <code>Memory | None</code> <p>The memory if found, otherwise <code>None</code>.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def get(self, memory_id: str) -&gt; Memory | None:\n    \"\"\"Retrieve a single memory by its ID.\n\n    Args:\n        memory_id: The unique identifier returned by :meth:`add`.\n\n    Returns:\n        The memory if found, otherwise ``None``.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.MemoryBackend.search","title":"search  <code>async</code>","text":"<pre><code>search(query: MemoryQuery) -&gt; list[MemoryResult]\n</code></pre> <p>Return memories ranked by relevance to the query.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>MemoryQuery</code> <p>Structured query containing text, filters, and limit.</p> required <p>Returns:</p> Type Description <code>list[MemoryResult]</code> <p>A list of results ordered by descending relevance score.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n    \"\"\"Return memories ranked by relevance to the query.\n\n    Args:\n        query: Structured query containing text, filters, and limit.\n\n    Returns:\n        A list of results ordered by descending relevance score.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.SupportsStreaming","title":"SupportsStreaming","text":"<p>               Bases: <code>Protocol</code></p> <p>Optional protocol for backends that can stream search results.</p>"},{"location":"api/protocols/#mnemo.protocols.SupportsStreaming.stream_search","title":"stream_search","text":"<pre><code>stream_search(\n    query: MemoryQuery,\n) -&gt; AsyncIterator[MemoryResult]\n</code></pre> <p>Yield results one at a time as they become available.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>MemoryQuery</code> <p>Structured query.</p> required <p>Yields:</p> Type Description <code>AsyncIterator[MemoryResult]</code> <p>Individual memory results in relevance order.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>def stream_search(self, query: MemoryQuery) -&gt; AsyncIterator[MemoryResult]:\n    \"\"\"Yield results one at a time as they become available.\n\n    Args:\n        query: Structured query.\n\n    Yields:\n        Individual memory results in relevance order.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.SupportsBulkOperations","title":"SupportsBulkOperations","text":"<p>               Bases: <code>Protocol</code></p> <p>Optional protocol for backends with efficient batch APIs.</p>"},{"location":"api/protocols/#mnemo.protocols.SupportsBulkOperations.add_many","title":"add_many  <code>async</code>","text":"<pre><code>add_many(memories: Sequence[Memory]) -&gt; list[str]\n</code></pre> <p>Persist multiple memories atomically.</p> <p>Parameters:</p> Name Type Description Default <code>memories</code> <code>Sequence[Memory]</code> <p>An ordered sequence of memories to store.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of IDs in the same order as the input.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def add_many(self, memories: Sequence[Memory]) -&gt; list[str]:\n    \"\"\"Persist multiple memories atomically.\n\n    Args:\n        memories: An ordered sequence of memories to store.\n\n    Returns:\n        A list of IDs in the same order as the input.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.SupportsBulkOperations.delete_many","title":"delete_many  <code>async</code>","text":"<pre><code>delete_many(memory_ids: Sequence[str]) -&gt; int\n</code></pre> <p>Delete multiple memories in one operation.</p> <p>Parameters:</p> Name Type Description Default <code>memory_ids</code> <code>Sequence[str]</code> <p>IDs to remove.</p> required <p>Returns:</p> Type Description <code>int</code> <p>The count of successfully deleted memories.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>async def delete_many(self, memory_ids: Sequence[str]) -&gt; int:\n    \"\"\"Delete multiple memories in one operation.\n\n    Args:\n        memory_ids: IDs to remove.\n\n    Returns:\n        The count of successfully deleted memories.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.BaseBackend","title":"BaseBackend","text":"<pre><code>BaseBackend()\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Internal ABC for backends shipped with mnemo.</p> <p>Third-party backends are NOT required to inherit from this class; they only need to satisfy :class:<code>MemoryBackend</code>.  This class exists to centralise validation, lifecycle hooks, and telemetry for the first-party backends.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>def __init__(self) -&gt; None:\n    self._closed = False\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.BaseBackend.aclose","title":"aclose  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Release resources.  Subclasses must call <code>self._closed = True</code>.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>@abc.abstractmethod\nasync def aclose(self) -&gt; None:\n    \"\"\"Release resources.  Subclasses must call ``self._closed = True``.\"\"\"\n    ...\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.BaseBackend.add","title":"add  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>add(memory: Memory) -&gt; str\n</code></pre> <p>Persist a memory and return its assigned ID.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>@abc.abstractmethod\nasync def add(self, memory: Memory) -&gt; str:\n    \"\"\"Persist a memory and return its assigned ID.\"\"\"\n    ...\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.BaseBackend.delete","title":"delete  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>delete(memory_id: str) -&gt; bool\n</code></pre> <p>Remove a memory by ID, returning True if it existed.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>@abc.abstractmethod\nasync def delete(self, memory_id: str) -&gt; bool:\n    \"\"\"Remove a memory by ID, returning True if it existed.\"\"\"\n    ...\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.BaseBackend.get","title":"get  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get(memory_id: str) -&gt; Memory | None\n</code></pre> <p>Retrieve a single memory by its ID.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>@abc.abstractmethod\nasync def get(self, memory_id: str) -&gt; Memory | None:\n    \"\"\"Retrieve a single memory by its ID.\"\"\"\n    ...\n</code></pre>"},{"location":"api/protocols/#mnemo.protocols.BaseBackend.search","title":"search  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>search(query: MemoryQuery) -&gt; list[MemoryResult]\n</code></pre> <p>Return memories ranked by relevance to the query.</p> Source code in <code>src/mnemo/protocols.py</code> <pre><code>@abc.abstractmethod\nasync def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n    \"\"\"Return memories ranked by relevance to the query.\"\"\"\n    ...\n</code></pre>"},{"location":"api/store/","title":"Store","text":""},{"location":"api/store/#mnemo.store.MemoryStore","title":"MemoryStore","text":"<pre><code>MemoryStore(backend: MemoryBackend)\n</code></pre> <p>Async-first, backend-agnostic memory store.</p> <p>Parameters:</p> Name Type Description Default <code>backend</code> <code>MemoryBackend</code> <p>Any object satisfying the :class:<code>~mnemo.protocols.MemoryBackend</code> protocol.</p> required Example <p>async def main(): ...     async with MemoryStore(backend=InMemoryBackend()) as store: ...         mid = await store.add(Memory(content=\"hello world\")) ...         result = await store.get(mid)</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def __init__(self, backend: MemoryBackend) -&gt; None:\n    self._backend = backend\n</code></pre>"},{"location":"api/store/#mnemo.store.MemoryStore.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Close the underlying backend and release all resources.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Close the underlying backend and release all resources.\"\"\"\n    await self._backend.aclose()\n</code></pre>"},{"location":"api/store/#mnemo.store.MemoryStore.add","title":"add  <code>async</code>","text":"<pre><code>add(memory: Memory) -&gt; str\n</code></pre> <p>Add a memory to the store.</p> <p>Parameters:</p> Name Type Description Default <code>memory</code> <code>Memory</code> <p>The memory to persist.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The unique ID assigned to the stored memory.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def add(self, memory: Memory) -&gt; str:\n    \"\"\"Add a memory to the store.\n\n    Args:\n        memory: The memory to persist.\n\n    Returns:\n        The unique ID assigned to the stored memory.\n    \"\"\"\n    return await self._backend.add(memory)\n</code></pre>"},{"location":"api/store/#mnemo.store.MemoryStore.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(memory_id: str) -&gt; bool\n</code></pre> <p>Delete a memory by ID.</p> <p>Parameters:</p> Name Type Description Default <code>memory_id</code> <code>str</code> <p>The ID to remove.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if deleted, <code>False</code> if not found.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def delete(self, memory_id: str) -&gt; bool:\n    \"\"\"Delete a memory by ID.\n\n    Args:\n        memory_id: The ID to remove.\n\n    Returns:\n        ``True`` if deleted, ``False`` if not found.\n    \"\"\"\n    return await self._backend.delete(memory_id)\n</code></pre>"},{"location":"api/store/#mnemo.store.MemoryStore.get","title":"get  <code>async</code>","text":"<pre><code>get(memory_id: str) -&gt; Memory | None\n</code></pre> <p>Retrieve a memory by ID.</p> <p>Parameters:</p> Name Type Description Default <code>memory_id</code> <code>str</code> <p>The ID returned by :meth:<code>add</code>.</p> required <p>Returns:</p> Type Description <code>Memory | None</code> <p>The memory, or <code>None</code> if not found.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def get(self, memory_id: str) -&gt; Memory | None:\n    \"\"\"Retrieve a memory by ID.\n\n    Args:\n        memory_id: The ID returned by :meth:`add`.\n\n    Returns:\n        The memory, or ``None`` if not found.\n    \"\"\"\n    return await self._backend.get(memory_id)\n</code></pre>"},{"location":"api/store/#mnemo.store.MemoryStore.search","title":"search  <code>async</code>","text":"<pre><code>search(query: MemoryQuery) -&gt; list[MemoryResult]\n</code></pre> <p>Search memories by semantic similarity and/or metadata filters.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>MemoryQuery</code> <p>A :class:<code>~mnemo.types.MemoryQuery</code> describing what to find.</p> required <p>Returns:</p> Type Description <code>list[MemoryResult]</code> <p>A ranked list of :class:<code>~mnemo.types.MemoryResult</code> objects.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>async def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n    \"\"\"Search memories by semantic similarity and/or metadata filters.\n\n    Args:\n        query: A :class:`~mnemo.types.MemoryQuery` describing what to find.\n\n    Returns:\n        A ranked list of :class:`~mnemo.types.MemoryResult` objects.\n    \"\"\"\n    return await self._backend.search(query)\n</code></pre>"},{"location":"api/store/#mnemo.store.SyncMemoryStore","title":"SyncMemoryStore","text":"<pre><code>SyncMemoryStore(backend: MemoryBackend)\n</code></pre> <p>Synchronous fa\u00e7ade over :class:<code>MemoryStore</code>.</p> <p>Intended for callers that cannot use <code>async</code>/<code>await</code> (e.g., Jupyter cells in <code>%autoawait off</code> mode, Django views, CLI scripts).</p> <p>Internally this wraps every call with <code>anyio.run()</code>, so it is compatible with both asyncio and Trio backends.</p> <p>Parameters:</p> Name Type Description Default <code>backend</code> <code>MemoryBackend</code> <p>Any object satisfying :class:<code>~mnemo.protocols.MemoryBackend</code>.</p> required Example <p>store = SyncMemoryStore(backend=InMemoryBackend()) with store: ...     mid = store.add(Memory(content=\"hello\")) ...     mem = store.get(mid)</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def __init__(self, backend: MemoryBackend) -&gt; None:\n    self._async_store = MemoryStore(backend)\n</code></pre>"},{"location":"api/store/#mnemo.store.SyncMemoryStore.add","title":"add","text":"<pre><code>add(memory: Memory) -&gt; str\n</code></pre> <p>Synchronous version of :meth:<code>MemoryStore.add</code>.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def add(self, memory: Memory) -&gt; str:\n    \"\"\"Synchronous version of :meth:`MemoryStore.add`.\"\"\"\n    return anyio.run(self._async_store.add, memory)\n</code></pre>"},{"location":"api/store/#mnemo.store.SyncMemoryStore.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close the underlying backend.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close the underlying backend.\"\"\"\n    anyio.run(self._async_store.aclose)\n</code></pre>"},{"location":"api/store/#mnemo.store.SyncMemoryStore.delete","title":"delete","text":"<pre><code>delete(memory_id: str) -&gt; bool\n</code></pre> <p>Synchronous version of :meth:<code>MemoryStore.delete</code>.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def delete(self, memory_id: str) -&gt; bool:\n    \"\"\"Synchronous version of :meth:`MemoryStore.delete`.\"\"\"\n    return anyio.run(self._async_store.delete, memory_id)\n</code></pre>"},{"location":"api/store/#mnemo.store.SyncMemoryStore.get","title":"get","text":"<pre><code>get(memory_id: str) -&gt; Memory | None\n</code></pre> <p>Synchronous version of :meth:<code>MemoryStore.get</code>.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def get(self, memory_id: str) -&gt; Memory | None:\n    \"\"\"Synchronous version of :meth:`MemoryStore.get`.\"\"\"\n    return anyio.run(self._async_store.get, memory_id)\n</code></pre>"},{"location":"api/store/#mnemo.store.SyncMemoryStore.search","title":"search","text":"<pre><code>search(query: MemoryQuery) -&gt; list[MemoryResult]\n</code></pre> <p>Synchronous version of :meth:<code>MemoryStore.search</code>.</p> Source code in <code>src/mnemo/store.py</code> <pre><code>def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n    \"\"\"Synchronous version of :meth:`MemoryStore.search`.\"\"\"\n    return anyio.run(self._async_store.search, query)\n</code></pre>"},{"location":"api/types/","title":"Types","text":""},{"location":"api/types/#mnemo.types.Memory","title":"Memory  <code>dataclass</code>","text":"<pre><code>Memory(\n    content: str,\n    metadata: dict[str, object] = _str_object_dict(),\n    embedding: list[float] | None = None,\n    memory_id: str | None = None,\n)\n</code></pre> <p>An immutable memory record.</p> <p>Parameters:</p> Name Type Description Default <code>content</code> <code>str</code> <p>The raw text (or serialised) content of the memory.</p> required <code>metadata</code> <code>dict[str, object]</code> <p>Arbitrary key-value pairs attached to this memory.</p> <code>_str_object_dict()</code> <code>embedding</code> <code>list[float] | None</code> <p>Optional pre-computed embedding vector.</p> <code>None</code> <code>memory_id</code> <code>str | None</code> <p>Optional caller-supplied ID; backends may override it.</p> <code>None</code>"},{"location":"api/types/#mnemo.types.MemoryQuery","title":"MemoryQuery  <code>dataclass</code>","text":"<pre><code>MemoryQuery(\n    text: str,\n    filters: dict[str, object] = _str_object_dict(),\n    limit: int = 10,\n    score_threshold: float = 0.0,\n)\n</code></pre> <p>A structured search query.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Natural-language text to search for.</p> required <code>filters</code> <code>dict[str, object]</code> <p>Key-value metadata filters (AND logic).</p> <code>_str_object_dict()</code> <code>limit</code> <code>int</code> <p>Maximum number of results to return.</p> <code>10</code> <code>score_threshold</code> <code>float</code> <p>Minimum similarity score (0.0-1.0).</p> <code>0.0</code>"},{"location":"api/types/#mnemo.types.MemoryResult","title":"MemoryResult  <code>dataclass</code>","text":"<pre><code>MemoryResult(memory: Memory, score: float)\n</code></pre> <p>A search result pairing a memory with its relevance score.</p> <p>Parameters:</p> Name Type Description Default <code>memory</code> <code>Memory</code> <p>The retrieved memory.</p> required <code>score</code> <code>float</code> <p>Similarity score in the range [0.0, 1.0].</p> required"},{"location":"api/backends/memory/","title":"In-Memory Backend","text":""},{"location":"api/backends/memory/#mnemo.backends.memory.InMemoryBackend","title":"InMemoryBackend","text":"<pre><code>InMemoryBackend()\n</code></pre> <p>               Bases: <code>BaseBackend</code></p> <p>A non-persistent in-process backend backed by a plain dict.</p> <p>Thread-safe reads are fine; this backend is not designed for concurrent writes across OS threads.</p> Example <p>import anyio async def demo(): ...     async with InMemoryBackend() as backend: ...         mid = await backend.add(Memory(content=\"hello\")) ...         mem = await backend.get(mid) ...         assert mem is not None</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>def __init__(self) -&gt; None:\n    super().__init__()\n    self._store: dict[str, Memory] = {}\n</code></pre>"},{"location":"api/backends/memory/#mnemo.backends.memory.InMemoryBackend.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Clear the store and mark the backend as closed.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Clear the store and mark the backend as closed.\"\"\"\n    self._store.clear()\n    self._closed = True\n</code></pre>"},{"location":"api/backends/memory/#mnemo.backends.memory.InMemoryBackend.add","title":"add  <code>async</code>","text":"<pre><code>add(memory: Memory) -&gt; str\n</code></pre> <p>Persist a memory and return its assigned ID.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def add(self, memory: Memory) -&gt; str:\n    \"\"\"Persist a memory and return its assigned ID.\"\"\"\n    self._check_not_closed()\n    mid = memory.memory_id or str(uuid.uuid4())\n    # Store a fresh copy with the assigned ID\n    self._store[mid] = Memory(\n        content=memory.content,\n        metadata=memory.metadata,\n        embedding=memory.embedding,\n        memory_id=mid,\n    )\n    return mid\n</code></pre>"},{"location":"api/backends/memory/#mnemo.backends.memory.InMemoryBackend.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(memory_id: str) -&gt; bool\n</code></pre> <p>Remove a memory by ID, returning True if it existed.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def delete(self, memory_id: str) -&gt; bool:\n    \"\"\"Remove a memory by ID, returning True if it existed.\"\"\"\n    self._check_not_closed()\n    return self._store.pop(memory_id, None) is not None\n</code></pre>"},{"location":"api/backends/memory/#mnemo.backends.memory.InMemoryBackend.get","title":"get  <code>async</code>","text":"<pre><code>get(memory_id: str) -&gt; Memory | None\n</code></pre> <p>Retrieve a single memory by its ID.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def get(self, memory_id: str) -&gt; Memory | None:\n    \"\"\"Retrieve a single memory by its ID.\"\"\"\n    self._check_not_closed()\n    return self._store.get(memory_id)\n</code></pre>"},{"location":"api/backends/memory/#mnemo.backends.memory.InMemoryBackend.search","title":"search  <code>async</code>","text":"<pre><code>search(query: MemoryQuery) -&gt; list[MemoryResult]\n</code></pre> <p>Na\u00efve substring search \u2014 replace with vector similarity in real backends.</p> Source code in <code>src/mnemo/backends/memory.py</code> <pre><code>async def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n    \"\"\"Na\u00efve substring search \u2014 replace with vector similarity in real backends.\"\"\"\n    self._check_not_closed()\n    results: list[MemoryResult] = []\n    for memory in self._store.values():\n        # Apply metadata filters\n        if not all(memory.metadata.get(k) == v for k, v in query.filters.items()):\n            continue\n        # Na\u00efve scoring: 1.0 if substring match, 0.5 otherwise\n        score = 1.0 if query.text.lower() in memory.content.lower() else 0.5\n        if score &gt;= query.score_threshold:\n            results.append(MemoryResult(memory=memory, score=score))\n\n    results.sort(key=lambda r: r.score, reverse=True)\n    return results[: query.limit]\n</code></pre>"},{"location":"guides/async/","title":"Async and Sync APIs","text":""},{"location":"guides/async/#async-recommended","title":"Async (recommended)","text":"<p>Mnemo is async-first.  Use it inside any <code>async def</code> function:</p> <pre><code>async with MemoryStore(backend=InMemoryBackend()) as store:\n    mid = await store.add(Memory(content=\"hello\"))\n</code></pre> <p>It is built on AnyIO, so it works equally well with asyncio and Trio event loops.</p>"},{"location":"guides/async/#sync-shim","title":"Sync shim","text":"<p>If you cannot use <code>async</code>/<code>await</code> (Django views, CLI scripts, Jupyter without <code>%autoawait</code>), use <code>SyncMemoryStore</code>:</p> <pre><code>from mnemo.store import SyncMemoryStore\nfrom mnemo.backends import InMemoryBackend\nfrom mnemo.types import Memory, MemoryQuery\n\nwith SyncMemoryStore(backend=InMemoryBackend()) as store:\n    mid = store.add(Memory(content=\"hello\"))\n    results = store.search(MemoryQuery(text=\"hello\"))\n</code></pre> <p>Warning</p> <p><code>SyncMemoryStore</code> starts a new event loop per call via <code>anyio.run()</code>. Do not call it from inside a running async event loop \u2014 use the async API instead.  For mixed codebases, consider <code>asyncer.syncify</code> which bridges into a background thread pool.</p>"},{"location":"guides/backends/","title":"Writing a Custom Backend","text":"<p>Any class satisfying the <code>MemoryBackend</code> Protocol can be used as a backend. You do not need to import or subclass anything from <code>mnemo</code>.</p> <pre><code>from mnemo.types import Memory, MemoryQuery, MemoryResult\n\n\nclass MyRedisBackend:\n    \"\"\"Example custom backend \u2014 satisfies MemoryBackend structurally.\"\"\"\n\n    def __init__(self, url: str) -&gt; None:\n        import redis.asyncio as redis\n        self._client = redis.from_url(url)\n\n    async def add(self, memory: Memory) -&gt; str:\n        import uuid, json\n        mid = memory.memory_id or str(uuid.uuid4())\n        await self._client.set(mid, json.dumps({\"content\": memory.content, \"meta\": memory.metadata}))\n        return mid\n\n    async def get(self, memory_id: str) -&gt; Memory | None:\n        import json\n        raw = await self._client.get(memory_id)\n        if raw is None:\n            return None\n        data = json.loads(raw)\n        return Memory(content=data[\"content\"], metadata=data[\"meta\"], memory_id=memory_id)\n\n    async def search(self, query: MemoryQuery) -&gt; list[MemoryResult]:\n        # Production: use Redis Search / vector similarity here\n        return []\n\n    async def delete(self, memory_id: str) -&gt; bool:\n        return bool(await self._client.delete(memory_id))\n\n    async def aclose(self) -&gt; None:\n        await self._client.aclose()\n</code></pre> <p>Then pass it to <code>MemoryStore</code>:</p> <pre><code>from mnemo import MemoryStore\n\nstore = MemoryStore(backend=MyRedisBackend(\"redis://localhost:6379\"))\n</code></pre> <p>Runtime protocol check</p> <p>You can verify your backend satisfies the protocol at runtime: <pre><code>from mnemo.protocols import MemoryBackend\nassert isinstance(MyRedisBackend(\"redis://localhost\"), MemoryBackend)\n</code></pre></p>"},{"location":"guides/installation/","title":"Installation","text":"<pre><code>pip install mnemo\n# or with uv:\nuv add mnemo\n</code></pre>"},{"location":"guides/installation/#optional-backend-extras","title":"Optional backend extras","text":"<pre><code>pip install \"mnemo[redis]\"      # Redis backend\npip install \"mnemo[pgvector]\"   # PostgreSQL + pgvector backend\npip install \"mnemo[openai]\"     # OpenAI embeddings support\n</code></pre>"},{"location":"guides/quickstart/","title":"Quick Start","text":"<pre><code>import asyncio\nfrom mnemo import MemoryStore\nfrom mnemo.backends import InMemoryBackend\nfrom mnemo.types import Memory, MemoryQuery\n\nasync def main() -&gt; None:\n    async with MemoryStore(backend=InMemoryBackend()) as store:\n        # Add memories\n        await store.add(Memory(content=\"Paris is the capital of France\", metadata={\"topic\": \"geography\"}))\n        await store.add(Memory(content=\"Berlin is the capital of Germany\", metadata={\"topic\": \"geography\"}))\n\n        # Semantic search (with the InMemoryBackend: substring match)\n        results = await store.search(MemoryQuery(text=\"capital\", limit=5))\n        for r in results:\n            print(f\"[{r.score:.2f}] {r.memory.content}\")\n\nasyncio.run(main())\n</code></pre>"}]}